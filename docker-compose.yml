services:
  auction-server:
    build:
      context: ./server
    container_name: auction-server
    ports:
      - "5000:5000"
    environment:
      SERVER_PORT: 5000
      AUCTION_DURATION_SECONDS: 30
